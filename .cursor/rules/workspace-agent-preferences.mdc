---
description: Workspace overrides — prefer compile checks and task-driven shell; no test workflow rules here.
alwaysApply: true
---

# go-code workspace — agent preferences

When anything here **conflicts** with generic Cursor User Rules, **prefer this file** for work under this workspace.

## Shell / commands

- Run terminal commands when they **directly advance** the task (for example `go build ./...`, a one-off repro, or a check the user asked for).
- If a command failed once, retrying or trying an alternative is still fine when it helps the user.

For `goclaw/`, follow `.cursor/rules/workflow.mdc` at this repo root.
